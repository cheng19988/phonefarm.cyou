/** Hybrid B2B: quote-first storefront with optional direct checkout for standard SKUs. */
export const PUBLIC_CHECKOUT_ENABLED = true;

/** Hide cart/account from nav; /cart and checkout URLs still work for direct links. */
export const PUBLIC_CART_IN_NAV = false;
